<resources xmlns:tools="http://schemas.android.com/tools">
    Base application theme.

    <style name="Base.Theme.BusinessTaxCalculator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@android:color/black</item> <!-- Чорний колір статус-бара -->
        <item name="android:windowLightStatusBar">false</item> <!-- Темний текст/іконки -->
    </style>

    <style name="CalendarDateTextStyle" parent="TextAppearance.MaterialComponents.Body1">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/sp10</item>
    </style>

    <style name="CalendarWeekDayTextStyle" parent="TextAppearance.MaterialComponents.Body2">
        <item name="android:textColor">@color/d9E9E9E</item>
        <item name="android:textSize">@dimen/sp12</item>
    </style>
</resources>